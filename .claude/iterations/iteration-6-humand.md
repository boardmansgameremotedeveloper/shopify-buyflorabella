1) duplicate the multicolumn section.
2) the intention is to duplicate this to allow up to 6-areas.
3) each area has a featured image, along with a small little icon which appears over the image.  there is a heading text underneath the image, as well as some description text.
4) above all images is a heading, centered.
5) above all images underneath the heading is a description.
6) there is a custom background image and/or overlay which fills the entire content area.
7) we want this background image to be optional, with a selectable and modifiable background color.
8) everything should be configurable inside the shopify CMS.
9) the result is a new shopify section and liquid file with 'birdlabs-' prefix.