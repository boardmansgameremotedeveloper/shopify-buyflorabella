there are two missing sections:

1) a small image gallery.  this should have up to six thumnail images with optional links to pages.  when mouseover there is a small effect to raise the image which is being moused over.  The alt text reads "featured highlight"

2) a simple section with all content adjustable to justify left/right/center/top.  It needs a heading.  It needs a description.  There are two buttons, each of which have an optional "raise up" effect when mouseover.  The background color for the buttons can be a solid color, or a gradient.  each button has a link somewhere, with editable button text.


3) implement both of these as new custom sections with a prefix of 'birdlabs-'