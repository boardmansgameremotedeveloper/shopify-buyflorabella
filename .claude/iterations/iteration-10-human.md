1) there is a page for 'community' which uses a custom page template.
2) this template currently uses a section named 'community gallery'.  we want to hijack this section to create a new image gallery.
3) The gallery should have a heading text.  The gallery should have a description text.  The justification should be able to be left/right/top/center/bottom.  the nine possible quadrants for alignment.
4) create, or point out an existing shopify section which will implement: a heading, a descriptive text and a callout area with a rounded corner border and three "statistics" for users to digest.  the stats are very brief.  example: "50+" (heading) and "Years in Use" (description).  each callout area has a heading and description.  the size of the heading, as well as the color is configurable, as well as the color for the descriptions.  create a new custom section with prefix: 'birdlabs-'
5) the "image gallery" section should be completely separate and custom named 'birdlabs-' to differentiate it.
6) make the new sections for this 'community' page template.
